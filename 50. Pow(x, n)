class Solution {
public:
    double myPow(double x, int n) {
        if(n==0) return  1;
        else if(n>0)
        {
            x= pow(x,n);
            return x;
        }
        else 
        {
            x=pow(x,n);
            
            return x;
        }
        
    }
};
